function Card({ users }) {

    return(
        <div className="card">
            {/* {users.map( user => 
                <img src={user.picture.medium} className="card-img-top" alt={user.name.first}{user.name.last} />
                <div className="card-body">
                    <h5 className="card-title">{user.name.first}{user.name.last}</h5>
                </div>
                <div className="card-footer">
                    <small className="text-muted">Last updated 3 mins ago</small>
                </div>
            )} */}
        </div>
    )
}

export default Card;